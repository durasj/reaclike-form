{
  "name": "reactlike-form",
  "version": "1.0.0",
  "description": "Dummy, zero dependencies form with custom react-like rendering.",
  "main": "src/index.html",
  "scripts": {
    "start": "parcel src/index.html --port 8080",
    "build": "parcel build src/index.html",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Jakub Duras<jakub@duras.me>",
  "license": "MIT",
  "devDependencies": {
    "parcel-bundler": "^1.11.0"
  }
}
